# add_mentor_file_usage

Aplica novo campo `mentorFileQuestionsCount` em `UsageMonthly` e cria a tabela `MentorAttachment`.

Comando para rodar (executar manualmente):

```
cd backend
npx prisma migrate dev --name add_mentor_file_usage
```
